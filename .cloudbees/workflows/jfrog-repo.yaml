apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: subhradeep-workflow-simple
on:
  workflow_dispatch:
jobs:
  my-simple-job:
    steps:
      - name: Step A
        uses: cloudbees-io/checkout@v1

      - name: Configure container registry for ECR
        uses: cloudbees-io/configure-ecr-credentials@v1

      - name: Step B
        uses: cloudbees-io/checkout@v1
