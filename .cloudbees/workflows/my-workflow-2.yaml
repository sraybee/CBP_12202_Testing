apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://nsrinivasa/cpb26351:latest
        shell: sh
        run: |
          echo "hello world"
