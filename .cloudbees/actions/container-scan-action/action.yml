apiVersion: automation.cloudbees.io/v1alpha1
kind: action
name: container-scan-action
description: Scans container for vulnerabilities (nested action called by security-action)

runs:
  using: composite
  steps:
    - name: Scan Base Images
      uses: docker://alpine:3.18
      shell: sh
      run: echo "Scanning images..."

    - name: Check for CVEs
      uses: docker://alpine:3.18
      shell: sh
      run: echo "Checking CVEs..."
